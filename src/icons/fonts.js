import * as React from "react";
import css from "../App.css";

export const Amatic_SC = ({ fontFam = "Amatic_SC" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Bungee_Shade = ({ fontFam = "Bungee_Shade" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Lobster_Two = ({ fontFam = "Lobster_Two" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Charmonman = ({ fontFam = "Charmonman" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Coiny = ({ fontFam = "Coiny" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Righteous = ({ fontFam = "Righteous" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Creepster = ({ fontFam = "Creepster" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Special_Elite = ({ fontFam = "Special_Elite" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Cabin_Sketch = ({ fontFam = "Cabin_Sketch" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Luckiest_Guy = ({ fontFam = "Luckiest_Guy" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Faster_One = ({ fontFam = "Faster_One" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Sacramento = ({ fontFam = "Sacramento" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Fredericka_the_Great = ({ fontFam = "Fredericka_the_Great" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Gaegu = ({ fontFam = "Gaegu" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Gamja_Flower = ({ fontFam = "Gamja_Flower" }) => (
  <div className={fontFam}> House Stark Our way is the old way</div>
);

export const Crafty_Girls = ({ fontFam = "Crafty_Girls" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Gloria_Hallelujah = ({ fontFam = "Gloria_Hallelujah" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Indie_Flower = ({ fontFam = "Indie_Flower" }) => (
  <div className={fontFam}> House Stark Our way is the old way</div>
);

export const Lato = ({ fontFam = "Lato" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Lobster = ({ fontFam = "Lobster" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Permanent_Marker = ({ fontFam = "Permanent_Marker" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Mountains_of_Christmas = ({
  fontFam = "Mountains_of_Christmas"
}) => <div className={fontFam}> House Stark Our way is the old way </div>;

export const Oswald = ({ fontFam = "Oswald" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Poiret_One = ({ fontFam = "Poiret_One" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Poppins = ({ fontFam = "Poppins" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Raleway = ({ fontFam = "Raleway" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Rock_Salt = ({ fontFam = "Rock_Salt" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Stalemate = ({ fontFam = "Stalemate" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Berkshire_Swash = ({ fontFam = "Berkshire_Swash" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);

export const Unlock = ({ fontFam = "Unlock" }) => (
  <div className={fontFam}> House Stark Our way is the old way </div>
);
